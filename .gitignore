# private: the probe battery never enters the public repo
battery/
# private: raw model outputs can echo probes
raw/
# internal task tracking
.beads/
AGENTS.md
# python
__pycache__/
*.pyc
# local env / keys
.env
# beads tracker infra
.gitattributes

# base64 of the private battery secrets - never commit
ops/SEISMO_*.txt
