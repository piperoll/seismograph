# private: the probe battery never enters the public repo
battery/
# private: raw model outputs can echo probes
raw/
# internal task tracking
.beads/
AGENTS.md
# python
__pycache__/
*.pyc
# local env / keys
.env
# beads tracker infra
.gitattributes

# internal ops workspace - held incident drafts (0054/0055), postmortems, the
# base64 battery secrets: NONE of ops/ is ever meant for the public repo.
# ignore the whole dir so a `git add -A` (esp. at the public flip) cannot leak it.
ops/
